server:
  port: 8081

spring:
  datasource:
    url: jdbc:sqlite::resource:db/manager.sqlite3
    driver-class-name: org.sqlite.JDBC




  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

